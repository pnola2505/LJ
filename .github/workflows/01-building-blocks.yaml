# This is a basic workflow to help you get started with Actions
name: 01-building-blocks

on: 
 push:
 pull_request:
 workflow_dispatch:



jobs: 
   build:
       runs-on: ubuntu-latest
       steps: 
          - name: checkout code
            uses: actions/checkout@v4
          - name: pulling folders
            run: ls
          
   secoundjob:
         runs-on: ubuntu-latest
         steps:
           - name : Multi-line bash
             run: |
                  echo "I am"
                  echo "a multi-line"
                  echo "script."
            
  
            
         
        
       
              





